package L02DataTypesandVariables.MoreExercise;

import java.util.Scanner;

public class T02FromLefttoTheRight {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = Integer.parseInt(scanner.nextLine());
        int sumOfDigits = 0;
        for (int i = 0; i < n; i++) {
//            String input = scanner.nextLine();
//            String[] a = input.split("\\s+");
//            int size = a.length;
//            long[] arr = new long[size];
//
//            for (int j = 0; j < size; j++) {
//                arr[j] = Long.parseLong(a[j]);
//            }
//            long max = Math.max(arr[0], arr[1]);
//            String maxed = "" + max;
//            for (int j = 0; j < maxed.length(); j++) {
//                char ch = maxed.charAt(j);
//                int c = Character.getNumericValue(ch);
//                sumOfDigits += c;
//            }
//            System.out.println(sumOfDigits);
//            sumOfDigits = 0;
        }
    }
}
